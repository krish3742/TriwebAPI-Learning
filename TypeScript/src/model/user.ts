const insertUserToDB = (userData: object) => {
    let data = userData;
    return "User registered!";
};

const findUserFromDB = (userData: object) => {
    let data = userData;
    return "User found!";
};

const updateUserToDB = (userData: object) => {
    let data = userData;
    return "User updated!";
};

const deleteUserFromDB = (userData: object) => {
    let data = userData;
    return "User deleted!";
};

export {insertUserToDB, findUserFromDB, updateUserToDB, deleteUserFromDB};